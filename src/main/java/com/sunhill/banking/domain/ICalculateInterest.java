package com.sunhill.banking.domain;

public interface ICalculateInterest {
	
	/**
	 * payInterest to calcualte a given parameter(termInYears)
	 * 
	 * @param termInYears
	 */
	void payInterest(int termInYears);
}
